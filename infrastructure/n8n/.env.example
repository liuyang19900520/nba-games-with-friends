# n8n Docker Environment Configuration
# Copy this file to .env and fill in actual values

# ============================================
# N8N Host Configuration
# ============================================
N8N_DOMAIN=localhost

# ============================================
# Database Configuration (Your Postgres Database)
# ============================================
DB_HOST=your-postgres-host
DB_PORT=5432
DB_NAME=n8n
DB_USER=your-db-user
DB_PASSWORD=your-db-password

# ============================================
# n8n Encryption Key (Important!)
# ============================================
# Get from existing n8n: Check ~/.n8n/config for encryptionKey
# Or run: cat ~/.n8n/config | grep encryptionKey
# Must use the same key, otherwise saved credentials cannot be decrypted
N8N_ENCRYPTION_KEY=your-encryption-key-from-existing-n8n

# ============================================
# Optional Configuration
# ============================================
# N8N_BASIC_AUTH_USER=admin
# N8N_BASIC_AUTH_PASSWORD=your-password
