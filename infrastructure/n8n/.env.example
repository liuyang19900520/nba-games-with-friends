# n8n Docker 环境变量配置
# 复制此文件为 .env 并填写实际值

# ============================================
# 数据库配置 (你的Postgres数据库)
# ============================================
DB_HOST=your-postgres-host
DB_PORT=5432
DB_NAME=n8n
DB_USER=your-db-user
DB_PASSWORD=your-db-password

# ============================================
# n8n加密密钥 (重要！)
# ============================================
# 从现有n8n获取: 查看 ~/.n8n/config 文件中的 encryptionKey
# 或者运行: cat ~/.n8n/config | grep encryptionKey
# 必须使用相同的密钥，否则已保存的凭证将无法解密
N8N_ENCRYPTION_KEY=your-encryption-key-from-existing-n8n

# ============================================
# 可选配置
# ============================================
# N8N_BASIC_AUTH_USER=admin
# N8N_BASIC_AUTH_PASSWORD=your-password
